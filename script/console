#!/usr/bin/env ruby
ENV['RACK_ENV'] ||= 'development'
require 'bundler/setup'
Bundler.require(:default, ENV['RACK_ENV'])

ARGV.each do |group|
  puts "Bundler: require group :#{group}"
  Bundler.require(group.to_sym)
end

require File.expand_path('../../boot.rb', __FILE__)

Pry.start
