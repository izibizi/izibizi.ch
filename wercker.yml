box: ruby
build:
    steps:
        - bundle-install
        - script:
            name: rake:build
            code: rake build
deploy:
    steps:
        - script:
            name: rake:deploy:test
            code: rake deploy:test
